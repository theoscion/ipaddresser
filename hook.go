package main

// runHook handles running the HTTP hook when an IP address is first loaded or changed
func runHook(ipAddress string, firstQuery bool) {
	// If the HTTP hook is enabled, run the hook
	if config.Hook.Enabled {

	}
}
